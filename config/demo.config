# Store the terraform State File to an S3 Bucket
# It's assume you Already have the bucket in your account
# Look at my previous chapter code to create the bucket

bucket  = "Your-S3-Bucket-Name"
key    = "<yourprojectname>-demo-statefiles"
